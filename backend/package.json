{
  "name": "meiosis-backend",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "prisma generate",
    "dev": "node src/server.js",
    "dev:stable": "start-backend.bat",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:push": "prisma db push",
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "@prisma/client": "^6.5.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^2.1.1",
    "pdf-parse": "^2.4.5",
    "puppeteer": "^24.40.0"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  }
}
